package main

import (
  "testing"
  "fmt"
  "strings"
  "time"
  "os/exec"
  "os"
  "io/ioutil"
)

func TestMain(m *testing.M) {
  // cmd := exec.Command("/usr/bin/docker", "login", "-u", "_json_key", "-p", vargs.Token, vargs.Registry)
  // cmd.Dir = path + ":/var/lib/"
  // cmd.Stdout = os.Stdout
  // cmd.Stderr = os.Stderr
  // err := cmd.Run()
  // trace(cmd)
}
